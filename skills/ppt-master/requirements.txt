# PPT Master Dependencies / PPT Master 依赖
# =============================================
#
# Most tools use only the Python standard library. Below are optional dependencies.
# 大部分工具仅使用 Python 标准库，以下为可选依赖。
#
# Install / 安装方式：
#   pip install -r requirements.txt

# SVG to PPTX tool / SVG 转 PPTX 工具
python-pptx>=0.6.21

# Recorded narration / 录制计时和旁白
edge-tts>=7.2.8

# Office compatibility: SVG to PNG fallback (choose one)
svglib>=1.5.0
reportlab>=4.0.0

# PDF to Markdown / PDF 转 Markdown
PyMuPDF>=1.23.0

# Document to Markdown / 文档转 Markdown
mammoth>=1.6.0
markdownify>=0.11.6
ebooklib>=0.18
nbconvert>=7.0.0

# Excel to Markdown / Excel 转 Markdown
openpyxl>=3.1.0

# Image processing / 图片处理
Pillow>=9.0.0
numpy>=1.20.0

# Web to Markdown / 网页转 Markdown
requests>=2.31.0
beautifulsoup4>=4.12.0
curl_cffi>=0.7.0

# AI image generation / AI 图片生成
google-genai>=1.0.0
openai>=1.0.0

# SVG Editor / SVG 编辑器
flask>=3.0.0
